{
    "name": "frkgg",
    "workspaces": [
        "apps/*",
        "shared/*"
    ],
    "type": "module",
    "private": true,
    "scripts": {
        "lint": "bunx biome check .",
        "style": "bunx biome check --write .",
        "coverage:report": "bun test --coverage --coverage-reporter=lcov && genhtml coverage/lcov.info --output-directory coverage/html && open coverage/html/index.html",
        "test": "bun test",
        "typecheck": "bunx tsc --noEmit",
        "dev": "bun run ./scripts/dev.ts"
    },
    "devDependencies": {
        "@biomejs/biome": "2.4.6",
        "@types/bun": "^1.3.10",
        "concurrently": "^9.2.1",
        "drizzle-kit": "^0.31.10"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
